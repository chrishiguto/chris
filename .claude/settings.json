{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.rs) leptosfmt --quiet \"$f\" && rustfmt --edition 2021 \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Formatting (leptosfmt + rustfmt)..."
          }
        ]
      }
    ]
  }
}
